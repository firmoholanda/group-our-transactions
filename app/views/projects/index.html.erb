<% provide(:title, "grouped projects") %>

<h1>total hours: <%= @projects.sum(:hours) %> hs</h1>

<section class="projects">
  <%= render partial: "projects_table", locals: { projects: @projects, allow_change: true} %>
</section>

<div class="bttn">
  <%= button_to 'add new Project', new_project_path, method: :get, class: 'btn btn-primary' %>
  <%= button_to 'back', current_user, method: :get, class: 'btn btn-secondary' %>
<div>