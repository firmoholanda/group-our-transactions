<% provide(:title, "ungrouped projects") %>

<h1>total hours: <%= @projects_no_group.sum(:hours) %> hs</h1>

<section class="projects">
  <%= render partial: "projects_table", locals: { projects: @projects_no_group, allow_change: true} %>
</section>

<div class="bttn">
  <%= button_to 'add new Project', new_project_path, method: :get, class: 'btn btn-primary' %>
  <%= button_to 'back', current_user, method: :get, class: 'btn btn-secondary' %>
<div>